.row
  .col-sm-6
    .text-center
      %h1.general-login-title.in-gold-500= t('devise.fast.quick_session.title')
      = render 'devise/shared/facebook_authorize_link'
      %p.general-login-text or
    = simple_form_for(@old_customer,
                      as: resource_name,
                      url: user_fast_session_path,
                      method: :put,
                      html: { id: 'quick_session' }) do |f|
      .form-inputs
        %h5.grey-600= t('devise.fast.quick_session.message')
        = f.input :email
        = f.input :password do
          = f.input_field :password, class: 'form-control'
          = render 'devise/shared/new_password_link'
      .form-actions.actions.text-center
        = f.button :submit, t('devise.fast.quick_session.submit')
  .col-sm-6
    .text-center
      %h3.general-login-title.in-gold-500= t('devise.fast.quick_registration.title')
      = render 'devise/shared/facebook_authorize_link'
      %p.general-login-text or
    = simple_form_for(@new_customer,
                      as: resource_name,
                      method: :post,
                      url: user_fast_registration_path,
                      html: { id: 'quick_registration' }) do |f|
      %h5.grey-600= t('devise.fast.quick_registration.message')
      %p.general-login-text= t('devise.fast.quick_registration.message_small')
      .form-inputs
        = f.input :email
      .form-actions.actions.text-center
        = f.button :submit, t('devise.fast.quick_registration.submit')
